Always use Context7 MCP when I need library/API documentation, code generation, setup or configuration steps without me having to explicitly ask.

В конце работы всегда запускай проверку билда и тестов, чтобы убедиться, что все работает корректно. Если есть ошибки, сообщи о них и предложи возможные решения.

Всегда делай бэкап базы данных (`mix ecto.dump`) перед запуском миграций (`mix ecto.migrate`).
